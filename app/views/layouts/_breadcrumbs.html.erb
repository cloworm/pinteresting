<% if crumbs.any? %>
  <div class="container breadcrumbs-container">
    <div class="col s12">
      <% crumbs.each do |(title, uri)| %>
        <%= link_to title, uri, class: "breadcrumb" %>
      <% end %>
    </div>
  </div>
<% end %>
